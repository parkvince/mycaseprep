# Public client-side analytics identifiers. These are not secret, but keep
# production values in your deployment environment rather than source code.
NEXT_PUBLIC_GA_MEASUREMENT_ID="G-XXXXXXXXXX"
NEXT_PUBLIC_AMPLITUDE_API_KEY="your-amplitude-api-key"
