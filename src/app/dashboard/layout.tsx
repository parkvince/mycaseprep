import type { Metadata } from "next";

export const metadata: Metadata = { title: "Start a Case" };

export default function DashboardLayout({ children }: { children: React.ReactNode }) {
  return children;
}
